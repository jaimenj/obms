{% extends '::base.html.twig' %}

{% block body -%}
    <h1>SalesNote creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('salesnote') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
