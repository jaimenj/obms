{% extends 'AppBundle::layout.html.twig' %}

{% block mainarea -%}

<h1>Worker payroll creation</h1>

{{ form(form) }}

<a class="btn btn-default" href="{{ path('workerpayroll') }}">
	Back to the list
</a>

{% endblock %}
